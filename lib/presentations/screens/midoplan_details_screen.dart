import 'package:flutter/material.dart';

class MidoplanDetailScreen extends StatelessWidget {
  const MidoplanDetailScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Center(
        child: Text("Hello cubit"),
      ),
    );
  }
}
